export default function ({ app }) {
    if (process.browser) { 

        // console.log("event change running", app.$echo, app);
    }
        // console.log("event subscription happens", $echo);
        // $echo.channel('event')
        // .listen('RegistrationChange', (e) => {
        //     console.log("wow holy shit this works!", e);
        // });
    
}
